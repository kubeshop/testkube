apiVersion: testworkflows.testkube.io/v1
kind: TestWorkflow
metadata:
  name: robotframework-workflow-suite
  labels:
    core-tests: workflows-suite
    type: suite
    tool: robotframework
spec:
  container:
    resources:
      requests:
        cpu: 32m
        memory: 32Mi
  steps:
  - execute:
      parallelism: 2
      workflows:
      - name: robotframework-browser-smoke
