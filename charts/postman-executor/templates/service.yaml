apiVersion: v1
kind: Service
metadata:
  name: {{ include "postman-executor.fullname" . }}
  labels:
    {{- include "postman-executor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "postman-executor.selectorLabels" . | nindent 4 }}
