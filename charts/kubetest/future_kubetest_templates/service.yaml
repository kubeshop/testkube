apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubetest.fullname" . }}
  labels:
    {{- include "kubetest.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kubetest.selectorLabels" . | nindent 4 }}
