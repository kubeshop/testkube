name: Go

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:

  build:
    runs-on: ubuntu-latest
    env:
      GOPRIVATE: "github.com/kubeshop/*"

    steps:
    - name: Configure git for private modules
      env:
        TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
      run: git config --global url."https://fog1985:${TOKEN}@github.com".insteadOf "https://github.com"
      run: echo "Checking-TOKEN -- ${TOKEN}"
      run: echo "Checking GOPRIVATE -- ${GOPRIVATE}"

    - uses: actions/checkout@v2

    - name: Set up Go
      uses: actions/setup-go@v2
      with:
        go-version: 1.16

    - name: Build
      run: go build -v ./...

    - name: Test
      run: go test -v ./...
