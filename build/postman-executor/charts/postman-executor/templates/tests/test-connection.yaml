apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "postman-executor.fullname" . }}-test-connection"
  labels:
    {{- include "postman-executor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "postman-executor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
